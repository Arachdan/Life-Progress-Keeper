window.addEventListener('load', adjust_changed_elements);

function adjust_changed_elements() {
    if(screen.width <= 810 && screen.height <= 1080) {
        // Input required changes here.
        
        
    }
}